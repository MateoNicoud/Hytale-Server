# =====================
# Network configuration
# =====================

# IP address the server listens on.
# 0.0.0.0 means "listen on all network interfaces" (recommended).
HYTALE_BIND_IP=0.0.0.0

# UDP port used by the Hytale server.
# Make sure this port is open in your firewall.
HYTALE_PORT=5520


# =====================
# Java (memory & runtime)
# =====================

# Minimum amount of memory allocated to the server.
# Should usually be the same as JAVA_XMX for stable performance.
JAVA_XMS=4G

# Maximum amount of memory the server can use.
# Do not set this higher than the available RAM on the machine.
JAVA_XMX=6G

# Garbage Collector tuning options.
# These settings are safe defaults optimized for long-running servers.
JAVA_GC_OPTS=-XX:+UseG1GC -XX:+ParallelRefProcEnabled -XX:G1HeapRegionSize=8m -XX:G1ReservePercent=20

# Additional Java options.
# Includes memory pre-allocation and garbage collection logging.
# Advanced users may customize this if needed.
JAVA_EXTRA_OPTS=-XX:+AlwaysPreTouch -XX:MaxGCPauseMillis=100 -Xlog:gc*:file=/var/log/hytale/gc.log:time,uptime,level,tags:filecount=5,filesize=20M


# =====================
# Server options
# =====================

# Enable Ahead-Of-Time (AOT) compilation if available.
# Improves startup time and runtime performance.
ENABLE_AOT=true

# Disable Sentry error reporting.
# Set to true to prevent sending crash or error reports.
DISABLE_SENTRY=false


# =====================
# Authentication & updates
# =====================

# Authentication mode:
# - authenticated : official online authentication (recommended)
# - offline       : no authentication (not recommended for public servers)
AUTH_MODE=authenticated

# Server release channel.
# Leave empty for stable releases.
# Example: pre-release
PATCHLINE=

# Skip update checks at startup.
# Set to true to completely disable update verification.
SKIP_UPDATE_CHECK=false


# =====================
# Additional server arguments
# =====================

# Extra command-line arguments passed directly to the server.
# Example:
# EXTRA_ARGS="--backup --backup-frequency 30"
EXTRA_ARGS=

# =====================
# Docker configuration
# =====================

# Docker Compose project name.
# This name is used to group Docker resources (containers, networks, volumes).
# It must be different when running multiple Compose projects on the same host.
COMPOSE_PROJECT_NAME=hytale-server
